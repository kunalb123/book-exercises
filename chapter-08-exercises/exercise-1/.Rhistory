my_breakfast` of everything you ate for breakfast
my_breakfast <- c("Cereal", "Milk", "Bananas")
print(my_breakfast)
my_breakfast` of everything you ate for breakfast
my_breakfast <- c("Cereal", "Milk", "Bananas")
print(my_breakfast)
# Create a vector `my_breakfast` of everything you ate for breakfast
my_breakfast <- c("Cereal", "Milk", "Bananas")
print(my_breakfast)
my_lunch <- "Sandwich"  # technically this is still a vector
is.vector(my_lunch)
meals <- list(breakfast = my_breakfast, lunch = my_lunch)
print(meals$breakfast)
my_breakfast <- c("Cereal", "Milk", "Bananas")
print(my_breakfast)
# Create a vector `my_lunch` of everything you ate (or will eat) for lunch
my_lunch <- "Sandwich"  # technically this is still a vector
is.vector(my_lunch)
# Create a list `meals` that has contains your breakfast and lunch
meals <- list(breakfast = my_breakfast, lunch = my_lunch)
# Add a "dinner" element to your `meals` list that has what you plan to eat
# for dinner
meals$dinner <- c("Soup", "Pasta")
<- c("Soup", "Pasta")
print(meals)
meals$dinner <- c("Soup", "Pasta")
print(meals)
meals[[5]] <- meals[[lunch]]
meals[[5]] <- meals$lunch
meals[[5]] <- meals[[lunch]]
meals[[5]] <- meals[["lunch"]]
early_meals <- c(meals[1], meals[2])
print(early_meals)
employees <- "Employee " + 1:100
employees <- paste("Employee", 1:100)
print(employees)
salaries <- runif(100, 40000, 50000)
salaries <- data.frame(employees, random_salaries, salary_adjustments, stringsAsFactors = FALSE)
employees <- paste("Employee", 1:100)
print(employees)
# Create a vector of 100 random salaries for the year 2017
# Use the `runif()` function to pick random numbers between 40000 and 50000
random_salaries <- runif(100, 40000, 50000)
# Create a vector of 100 annual salary adjustments between -5000 and 10000.
# (A negative number represents a salary decrease due to corporate greed)
# Again use the `runif()` function to pick 100 random numbers in that range.
salary_adjustments <- ruinif(100, -5000, 10000)
# Create a data frame `salaries` by combining the 3 vectors you just made
# Remember to set `stringsAsFactors=FALSE`!
salaries <- data.frame(employees, random_salaries, salary_adjustments, stringsAsFactors = FALSE)
random_salaries <- runif(100, 40000, 50000)
random_salaries <- runif(100, 40000, 50000)
random_salaries <- runif(100, 40000, 50000)
random_salaries <- runif(100, 40000, 50000)
random_salaries <- runif(100, 40000, 50000)
employees <- paste("Employee", 1:100)
print(employees)
# Create a vector of 100 random salaries for the year 2017
# Use the `runif()` function to pick random numbers between 40000 and 50000
random_salaries <- runif(100, 40000, 50000)
# Create a vector of 100 annual salary adjustments between -5000 and 10000.
# (A negative number represents a salary decrease due to corporate greed)
# Again use the `runif()` function to pick 100 random numbers in that range.
salary_adjustments <- runif(100, -5000, 10000)
# Create a data frame `salaries` by combining the 3 vectors you just made
# Remember to set `stringsAsFactors=FALSE`!
salaries <- data.frame(employees, random_salaries, salary_adjustments, stringsAsFactors = FALSE)
# Add a column to the `salaries` data frame that represents each person's
# salary in 2018 (e.g., with the salary adjustment added in).
# Add a column to the `salaries` data frame that has a value of `TRUE` if the
# person got a raise (their salary went up)
### Retrieve values from your data frame to answer the following questions
### Note that you should get the value as specific as possible (e.g., a single
### cell rather than the whole row!)
# What was the 2018 salary of Employee 57
# How many employees got a raise?
# What was the dollar value of the highest raise?
# What was the "name" of the employee who received the highest raise?
# What was the largest decrease in salaries between the two years?
# What was the name of the employee who recieved largest decrease in salary?
# What was the average salary change?
# For people who did not get a raise, how much money did they lose on average?
## Consider: do the above averages match what you expected them to be based on
## how you generated the salaries?
# Write a .csv file of your salary data to your working directory
employees <- paste("Employee", 1:100)
print(employees)
# Create a vector of 100 random salaries for the year 2017
# Use the `runif()` function to pick random numbers between 40000 and 50000
random_salaries <- runif(100, 40000, 50000)
# Create a vector of 100 annual salary adjustments between -5000 and 10000.
# (A negative number represents a salary decrease due to corporate greed)
# Again use the `runif()` function to pick 100 random numbers in that range.
salary_adjustments <- runif(100, -5000, 10000)
# Create a data frame `salaries` by combining the 3 vectors you just made
# Remember to set `stringsAsFactors=FALSE`!
salaries <- data.frame(employees, random_salaries, salary_adjustments, stringsAsFactors = FALSE)
salaries["adjusted_salary"] <- random_salaries + salary_adjustments
salaries["adjusted_salary"] <- random_salaries + salary_adjustments
salaries["raise_received"] <- random_salaries < salaries["adjusted_salary"]
print(salaries[57, "adjusted_salary"])
num_raises <- 1:100[salaries[, "raise_received"]]
source('~/Library/CloudStorage/iCloud Drive/Documents/INFO 201/book-exercises/chapter-10-exercises/exercise-2/exercise.R', echo=TRUE)
num_raises <- (1:100)[salaries[, "raise_received"]]
print(num_raises)
print(num_raises)
print(num_raises)
print(num_raises)
print(length(num_raises))
print(num_raises)
print(num_raises)
print(num_raises)
print(max(salaries[, "salary_adjustments"]))
print(hightes_raise)
write.csv(salaries, "salary_info.csv", row.names = FALSE)
print(avg(salaries[, "salary_adjustments"]))
print(aveage(salaries[, "salary_adjustments"]))
print(mean(salaries[, "salary_adjustments"]))
print(min(salaries[, "salary_adjustments"]))
write.csv(salaries, "salary_info.csv", row.names = FALSE)
